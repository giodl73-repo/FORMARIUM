# Pointer Entry Expansion 12 Plan

Date: 2026-08-18
Status: active internal simulation closeout plan

## Objective

Add `sim-64` with a final 10-row delta over V11, expanding the effective
Pointer Entry registry from 240 to 250 labels and closing the current reviewed
concordance-expansion sequence.

This is a 250-label generated pointer layer. It is not the R5 goal of
approximately 250 reviewed canonical Tables.

## Selection

Admit ten exact labels used by 3-4 owning Tables:

`arithmetic-mean`, `evaluation-scale`, `fraction`, `geometry`, `magnitude`,
`mathematical-function`, `median`, `parameter`, `responsibility`, and
`specification`.

Continue to defer generic `order`, `point`, and `type`; status or syntax
artifacts; morphological duplicates; and remaining sparse labels requiring a
new editorial rationale.

## Product value

The closeout batch adds direct routes for statistical summaries, mathematical
structure, evaluation scales, model parameters, organizational
responsibility, and specifications.

## Scope and stop

Require 250 effective rows, one exact V11 base, ten additions, nonzero use,
unchanged search, and reconstructable predecessors. Close the current
expansion sequence after validation; further admission requires a new census
and explicit editorial trigger.
