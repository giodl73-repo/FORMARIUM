# V2 Rolling Reference Integration Plan

Date: 2026-08-18
Status: active rolling candidate; explicitly not frozen by this action

## Objective

Make the three admitted post-V1 Factor Tables visible through canonical
interchange, assurance, catalog, book, search, and the next internal website:

1. Optimization Problem Structure Mapping;
2. Prototype, Test, and Iteration Procedure; and
3. Contribution, Credit, Priority, and Legacy Evidence.

## Revision policy

`factorium-reference-v2` uses the exact V1 record grammar and preserves every
V1 entry and view block byte-for-byte. V2 is the current rolling internal
candidate and remains open to later reviewed additions. This action does not
freeze V2, declare a release, close Factor Forge, or require a new revision for
each subsequent reviewed content batch.

V1 remains an immutable reproducibility baseline because prior campaign
digests, assurance records, and `sim-49` depend on its exact bytes. The user's
authorization removes the integration gate, not the value of historical
custody.

## Exact delta

- entries: 54 -> 54;
- senses: 419 -> 419;
- factors: 638 -> 638;
- views: 97 -> 100;
- relations: unchanged V0 sidecar;
- assurance bindings: 162 -> 165;
- generated projection: `sim-49` -> `sim-50` with three new routes.

## Product boundary

Reader: someone trying to structure an optimization problem, learn through a
prototype, or reconstruct contribution and credit.

Job: find the reviewed view through ordinary search and read it in the same
book/table system as existing content.

Evidence now: parser compatibility, exact block preservation, assurance
coverage, deterministic catalog/search/site generation, and browser mechanics.

Evidence later: comprehension, findability, decision improvement, return use,
and external-reader value.

Stop this action after the three paths are visible and validated. Do not add
new content, relations, automated decisions, reader claims, or a V2 freeze.
