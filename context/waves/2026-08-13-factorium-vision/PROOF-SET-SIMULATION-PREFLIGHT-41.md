# Proof Set Simulation Preflight 41

Status: complete

Edition: `sim-40`

## Goal and result

Repair one documented everyday-language lookup detour without changing the
reference or inferring user intent. Exact normalized `size`, `how big`, and
`how large` queries now show one conditional **If you mean geometric size**
route to the existing Geometric Measure canonical Table.

The cue is visibly labelled as navigation rather than synonym or
classification. It does not rewrite the query, change scores, reorder or
suppress matches, alter ownership grouping, or add canonical metadata. The
185-record search index is byte-identical to `sim-39`; near misses do not
activate.

Static and live Edge checks verify exact activation, one resolved target,
unchanged complete lexical rankings, conditional removal, zero missing
targets, and no mobile overflow. The edition remains 217 sources, 175
canonical records, 185 search records, eighteen chapters, and 239 pages. Site
identity is
`2cd79e84f912969f2338ac34c123eaffb02ab2442fcc50c0ca4ee772f7b9433f`;
standalone SHA-256 is
`8a0d991ef3b934028d2ced0fc4dda1adcc24f0449074b8037785e3e9795be4e7`.
`sim-39` reproduces exactly.

## Product boundary

This is one edition-local conditional route, not alias infrastructure, query
expansion, a canonical semantic relation, or a new entry. It proves mechanics
only, not findability, comprehension, preference, reader value, or preview
readiness. Stop unless another observed lookup failure justifies an explicit
route.
