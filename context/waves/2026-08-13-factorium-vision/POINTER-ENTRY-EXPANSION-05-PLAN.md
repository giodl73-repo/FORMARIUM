# Pointer Entry Expansion 05 Plan

Date: 2026-08-18
Status: active internal simulation plan

## Objective

Add a sixth bounded successor registry in `sim-57`, expanding the preserved
20-label sequence from 100 to 120 Pointer Entries.

## Selection

Admit 20 exact labels used by 5-9 owning Tables:

`activity`, `aggregation`, `amount`, `classification`, `confidence`,
`denominator`, `distribution`, `effect`, `entity`, `error`, `evaluation`,
`inclusion`, `interface`, `interpretation`, `location`, `ordering`, `process`,
`support`, `timing`, and `transfer`.

Defer `exclusion` because it appears in only four owners and defer
`temperature` for a later domain-focused review. Generic, morphological, and
path-colliding exclusions remain unchanged.

## Product value

The batch adds navigation across evaluation, quantity structure, interfaces,
interpretation, evidence support, timing, and transfer while preserving exact
authored owner contexts.

## Scope and stop

Require exactly 120 rows, nonzero exact use, unchanged canonical search, source
custody, and reconstructable predecessors. Stop before automatic extraction,
morphology, synonym expansion, canonical promotion, or reader claims.
