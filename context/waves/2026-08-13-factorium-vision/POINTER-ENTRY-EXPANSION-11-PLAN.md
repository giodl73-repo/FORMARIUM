# Pointer Entry Expansion 11 Plan

Date: 2026-08-18
Status: active internal simulation plan

## Objective

Add `sim-63` with a 20-row delta over V10, expanding the effective Pointer
Entry registry from 220 to 240 labels.

## Selection

Admit 20 exact labels used by 2-6 owning Tables:

`causal-effect`, `causal-mechanism`, `choice`, `choreography`, `commitment`,
`configuration`, `estimand`, `iteration`, `milestone`, `missingness`, `noise`,
`nonconformity`, `orchestration`, `shape`, `temperature`, `transaction`,
`translation`, `weight`, `window`, and `workflow`.

Continue to defer generic `order`, `point`, and `type`; status or syntax
artifacts; and morphological duplicates.

## Product value

The batch adds routes for causal claim components, decentralized and directed
coordination, planning and iteration, data absence and noise, system
configuration, transactions, translation, and quantitative context.

## Scope and stop

Require 240 effective rows, one exact V10 base, 20 additions, nonzero use,
unchanged search, and reconstructable predecessors. Stop before automatic
admission, morphology, synonyms, canonical promotion, or reader claims.
