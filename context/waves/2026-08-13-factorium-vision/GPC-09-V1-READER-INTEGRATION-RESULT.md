# GPC-09 V1 Reader Integration Result

Date: 2026-08-18
Decision: adopt V1 and `sim-49` as the current internal projection

## Result

The admitted GPC-09 batch now crosses the complete publication path. The
`factorium-reference-v1` interchange contains 54 entries, 419 senses, 638
factors, and 97 views. Its 162-row assurance sidecar covers every entry and
view plus the unchanged eleven V0 relations. Live catalogs are deterministic
V1 projections.

`sim-49` adds exactly three Book records and three neutral route tasks. Its
178-record Book, 188-record search index, nineteen chapters, and 245 site
pages have zero missing targets. Ordinary searches for meaning and
interpretation, epistemic warrant and defeaters, or authority and consent
reach the new anchor and views. Book remains the default reader profile.

## Compatibility and custody

All 53 V0 entry blocks and 95 V0 view blocks survive exactly inside V1. The
V0 reference and assurance files, the V0 relation file, and the historical
`sim-48` supplement retain their frozen SHA-256 identities. Composition Query
traces remain V0-bound. V0 sidecars validate independently even though the
live generated catalogs now belong to V1.

The migration adds no relation, ranking behavior, authority decision,
reciprocal old-owner edit, or further content. The website remains a
disposable projection over canonical Tables and selected Book records.

## Role review

Eight roles produced 24 findings: zero P1, three P2 closed during
implementation, and 21 P3 notes. The closed findings repaired omitted V1
search/chapter inputs, replaced locale-dependent ordering with exact ASCII
ordering, and decoupled historical V0 sidecar validation from current V1
projection validation. The fixed point is approved for this internal
candidate publication boundary.

## Validation

- V1 generation, `reference-sync`, `reference-check`, and both V1 and V0
  sidecar checks pass.
- `sim-49` renders with exact source, selection, task, search, chapter, page,
  and link coverage.
- Headless browser checks pass three ordinary-language searches and a 390 px
  viewport without horizontal overflow.
- Rust formatting, lint, tests, the global `.roles` checker, and repository
  whitespace validation pass.

## Product disposition

Use V1 and `sim-49` as the current internal book/search/site projection, while
retaining V0 and `sim-48` as historical compatibility baselines. This closes
the reader-integration investment earned by GPC-09. It establishes mechanics
and coverage only: no external reader evidence, community endorsement, truth
service, permission decision, universality, or global completeness is claimed.
