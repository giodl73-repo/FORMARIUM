# Pointer Entry Expansion 08 Plan

Date: 2026-08-18
Status: active internal simulation plan

## Objective

Add `sim-60` with a 20-row delta over V7, expanding the effective Pointer Entry
registry from 160 to 180 labels.

## Selection

Admit 20 exact labels used by 4-5 owning Tables:

`assignment`, `composition`, `consumption`, `control`, `counterfactual`,
`equation`, `flow`, `governance`, `indicator`, `length`, `load`, `objective`,
`projection`, `regime`, `resistance`, `sampling`, `solution`, `speed`,
`threshold`, and `tolerance`.

Exclude adjectival `spatial` and retain all prior generic, morphology, syntax,
status, and path-collision exclusions.

## Product value

The batch adds routes for control and governance, sampling and indicators,
thresholds and tolerances, causal comparison, flow and resistance, and
mathematical or physical quantities.

## Scope and stop

Require 180 effective rows, one exact V7 base, 20 additions, nonzero use,
unchanged search, and reconstructable predecessors. Stop before automatic
admission, morphology, synonyms, canonical promotion, or reader claims.
