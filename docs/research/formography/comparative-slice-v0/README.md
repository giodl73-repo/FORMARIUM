# Formography Comparative Encoding Slice V0

Date: 2026-08-18
Status: three-case authored comparison packet complete

## Question

What does a candidate formograph retain beyond a conventional concept map and
a generic scoped property graph when all three encode the same governed
Factorium case?

## Owner-first boundary

The canonical owner remains:

- `reference/factorium-reference-v2.factorium`;
- `tables/entries/access-permission-authorization-entitlement.md`;
- `tables/constraints/access-authorization-integrity.md`;
- their digest-bound assurance records.

The comparison artifacts do not modify, replace, or extend that authority.
They are research projections over one frozen case.

## Artifacts

| Artifact | Purpose |
|---|---|
| `comparison-contract-v0.json` | predeclared features, profiles, and decision rule |
| `access-authorization-source-v0.json` | frozen source extraction and custody |
| `access-authorization-concept-map-v0.json` | concept-map comparison |
| `access-authorization-property-graph-v0.json` | fully expressive scoped graph comparison |
| `access-authorization-formograph-v0.json` | candidate Formography contract |
| `access-authorization-formograph-invalid-v0.json` | structured missing-loss failure |
| `comparison-result-v0.json` | retained result and narrowed verdict |

Validator:

```powershell
node tools\check_formography_comparative_slice.js
```

## Result

The concept map retains the focus question, primary decomposition, alternative
views, and labeled relationships, but not exact digest custody, review
bindings, qualifier contracts, boundary rules, unresolved state, or projection
loss as native requirements.

The generic property graph retains all ten predeclared features. This disproves
any claim that Formography is needed because graphs cannot encode the
information.

The candidate formograph also retains all ten features, but makes every one a
required, fail-closed method invariant. The invalid fixture is rejected with
`FG-PROJECTION-LOSS-MISSING`; the generic graph profile has no equivalent
native requirement.

Current conclusion:

`SURVIVES_AS_GOVERNED_METHOD_PROFILE_NOT_DISTINCT_GRAPH_REPRESENTATION`

Case 01 alone was not a field proof and required two additional cross-domain
cases plus independent coding.

## Case 02 - Amount, Concentration, and Composition

The quantitative case reuses the same ten core features and adds an explicit
seven-field `quantity-contract-v0` extension. Both the property graph and
formograph retain the complete core and extension. The formograph requires
the extension and rejects a missing denominator basis with
`FG-QUANTITY-BASIS-MISSING`.

Verdict:

`CORE_TRANSFERS_WITH_EXPLICIT_DOMAIN_EXTENSION_METHOD_PROFILE_REMAINS_NARROWED`

The core transferred without silent redefinition. At that stage, one
evaluative or interpretive case remained before independent coding.

## Case 03 - Choice, Alternative, and Selection

The evaluative case uses the same core and an explicit eight-field decision
extension. Both rich encodings retain the complete packet. The formograph
rejects a recommendation without separately declared selection authority with
`FG-DECISION-AUTHORITY-MISSING`.

Three-case verdict:

`THREE_CASE_CORE_SURVIVES_AS_GOVERNED_PROFILE_INDEPENDENT_CODING_REQUIRED`

Authored comparative encoding is now closed. See
`THREE-CASE-SYNTHESIS.md`. Independent coders must receive source records and
the frozen protocol rather than these authored outputs.
